{
  "name": "jcode-vscode",
  "displayName": "Jcode",
  "description": "Full Jcode agent wrapper: sidebar chat, editor context, session management, and slash commands over jcode's ACP adapter (`jcode acp`).",
  "version": "0.10.1",
  "publisher": "claudioj",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/jinclaudio/jcode-vscode.git"
  },
  "engines": {
    "vscode": "^1.95.0"
  },
  "categories": [
    "AI",
    "Machine Learning"
  ],
  "keywords": [
    "jcode",
    "vscode",
    "vscode-extension",
    "ai",
    "ai-coding",
    "coding-agent",
    "ai-agent",
    "developer-tools"
  ],
  "activationEvents": [],
  "main": "./extension.js",
  "contributes": {
    "commands": [
      {
        "command": "jcode.open",
        "title": "Jcode: Open Chat"
      },
      {
        "command": "jcode.diagnose",
        "title": "Jcode: Run Connection Diagnostics"
      },
      {
        "command": "jcode.openTerminal",
        "title": "Jcode: Open Terminal Agent"
      },
      {
        "command": "jcode.askSelection",
        "title": "Jcode: Ask About Selection"
      },
      {
        "command": "jcode.explainSelection",
        "title": "Jcode: Explain Selection"
      },
      {
        "command": "jcode.fixSelection",
        "title": "Jcode: Fix Selection"
      }
    ],
    "viewsContainers": {
      "activitybar": [
        {
          "id": "jcode",
          "title": "Jcode",
          "icon": "media/jcode.svg"
        }
      ]
    },
    "views": {
      "jcode": [
        {
          "type": "webview",
          "id": "jcode.chatView",
          "name": "Chat",
          "icon": "media/jcode.svg",
          "contextualTitle": "Jcode Chat"
        }
      ]
    },
    "menus": {
      "editor/context": [
        {
          "command": "jcode.askSelection",
          "group": "jcode@1",
          "when": "editorHasSelection"
        },
        {
          "command": "jcode.explainSelection",
          "group": "jcode@2",
          "when": "editorHasSelection"
        },
        {
          "command": "jcode.fixSelection",
          "group": "jcode@3",
          "when": "editorHasSelection"
        }
      ],
      "commandPalette": [
        {
          "command": "jcode.open"
        },
        {
          "command": "jcode.diagnose"
        },
        {
          "command": "jcode.openTerminal"
        },
        {
          "command": "jcode.askSelection"
        },
        {
          "command": "jcode.explainSelection"
        },
        {
          "command": "jcode.fixSelection"
        }
      ]
    },
    "keybindings": [
      {
        "command": "jcode.askSelection",
        "key": "ctrl+shift+j",
        "mac": "cmd+shift+j",
        "when": "editorTextFocus && editorHasSelection"
      }
    ],
    "configuration": {
      "title": "Jcode",
      "properties": {
        "jcode.executablePath": {
          "type": "string",
          "default": "jcode",
          "description": "Path to the Jcode executable."
        },
        "jcode.launchArguments": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [],
          "description": "Extra global arguments passed to Jcode chat runs and the interactive terminal."
        },
        "jcode.maxSelectionCharacters": {
          "type": "number",
          "default": 200000,
          "minimum": 1000,
          "description": "Maximum number of selected characters that may be sent to Jcode."
        },
        "jcode.contextWindowTokens": {
          "type": "number",
          "default": 0,
          "minimum": 0,
          "markdownDescription": "Override the context window used by the sidebar occupancy meter. `0` uses Jcode's model-family defaults (normally 200K, with known long-context models detected automatically)."
        },
        "jcode.defaultModel": {
          "type": "string",
          "default": "",
          "markdownDescription": "Default model applied to new Jcode chat sessions and the terminal agent, e.g. `deepseek-v4-pro`. When empty, Jcode's own default model is used. The sidebar chat has its own model picker that overrides this."
        },
        "jcode.models": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [],
          "markdownDescription": "Fallback model names for the sidebar chat model picker when Jcode's live model catalog is unreachable. When empty, a built-in curated list is used. The picker normally lists the real catalog from the connected Jcode daemon (`listModels`)."
        },
        "jcode.autoApprove": {
          "type": "boolean",
          "default": false,
          "markdownDescription": "Automatically allow permission prompts issued by the agent. When disabled, a VS Code dialog asks you to allow or deny each permission request during a turn."
        },
        "jcode.shareEditorContext": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "Include a compact summary of the active editor, selection, and open files with each sidebar message so the agent knows what you are looking at in VS Code."
        },
        "jcode.multiSession.enabled": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "Enable the multi-task dashboard and parallel Jcode sessions."
        },
        "jcode.multiSession.maxConcurrent": {
          "type": "number",
          "default": 3,
          "minimum": 1,
          "maximum": 12,
          "markdownDescription": "Maximum number of Jcode task sessions that may run concurrently. Additional tasks remain queued."
        },
        "jcode.multiSession.defaultIsolation": {
          "type": "string",
          "enum": [
            "worktree",
            "shared",
            "read-only"
          ],
          "default": "worktree",
          "markdownDescription": "Default workspace mode for new tasks. Worktree creates an isolated Git branch and directory; shared uses the current workspace; read-only instructs the agent not to edit files."
        },
        "jcode.multiSession.autoCommit": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "Automatically commit completed worktree task changes on their isolated task branch."
        },
        "jcode.defaultEffort": {
          "type": "string",
          "enum": [
            "",
            "none",
            "minimal",
            "low",
            "medium",
            "high",
            "xhigh",
            "max"
          ],
          "default": "",
          "markdownDescription": "Default reasoning effort applied to new Jcode chat sessions (`setReasoningEffort`) and passed to the terminal agent via the `JCODE_OPENAI_REASONING_EFFORT` / `JCODE_ANTHROPIC_REASONING_EFFORT` environment variables. When empty, Jcode's own default is used. Levels: `none`, `minimal`, `low`, `medium`, `high`, `xhigh`, `max`; the accepted set depends on the provider."
        }
      }
    }
  },
  "scripts": {
    "check": "node --check extension.js && node --check task-manager.js && node --check acp-client.js && node --check media/chat.js",
    "check:webview": "node scripts/check-webview.js",
    "test:acceptance": "node scripts/run-acceptance.js",
    "package": "npx @vscode/vsce package"
  },
  "dependencies": {},
  "devDependencies": {
    "@vscode/vsce": "^3.6.0"
  }
}
